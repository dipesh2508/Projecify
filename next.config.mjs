/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["images.unsplash.com", "images.pexels.com", "lh3.googleusercontent.com"],
    },
};

export default nextConfig;
